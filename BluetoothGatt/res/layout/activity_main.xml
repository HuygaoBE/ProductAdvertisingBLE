<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal">


    <TextView
        android:id="@+id/texttitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="BLE Global"
        android:textSize="50dp"
        android:textStyle="bold"
        android:textColor="#99FF99"
        android:layout_marginTop="20dp"
        />

    <pl.droidsonroids.gif.GifImageView
        android:id="@+id/image_gif"
        android:layout_width="match_parent"
        android:layout_below="@id/texttitle"
        android:layout_marginTop="50dp"
        android:layout_height="350dp"
        android:src="@drawable/image_gif2" />

    <Button
        android:id="@+id/btnView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/image_gif"
        android:layout_marginTop="50dp"
        android:background="@drawable/button_style"
        android:text="Xem danh sách quảng cáo"
        android:textSize="24dp" />

    <Button
        android:id="@+id/btnDelete"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/btnView"
        android:layout_marginTop="20dp"
        android:background="@drawable/button_style"
        android:text="Xóa danh sách quảng cáo"
        android:textSize="24dp" />

</RelativeLayout>